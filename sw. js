const CACHE_NAME = 'battle-of-glory-v1';
const urlsToCache = ['/', '/index.html', '/src/css/styles.css'];

self.addEventListener('install', (event) => {
  event.waitUntil(
    caches.open(CACHE_NAME).then((cache) => cache.addAll(urlsToCache))
  );
});
